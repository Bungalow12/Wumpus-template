import { HighScore } from './HighScore';

describe('HighScore', () => {
  test('Placeholder test please remove', () => {
    const highScore: HighScore = new HighScore();
    expect(highScore).toBeDefined();
  });
});