import GameControl from './GameControl';

describe('GameControl', () => {
  test('Placeholder test please remove', () => {
    const gameControl: GameControl = new GameControl();
    expect(gameControl).toBeDefined();
  });
});
